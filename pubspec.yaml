name: input_form_field
description: Input form field is a StatefulWidget that contains a TextFormField. A handy widget aiming to simplify complexity of default TextFormField widget.
version: 0.0.6
homepage: https://github.com/momshaddinury/input_form_field
repository: https://github.com/momshaddinury/input_form_field


environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/demo.png